import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const Search2 = () => {

  return (
    <View style={styles.aboutWrapper}>
      <Text style={styles.createAccountText}>Search2</Text>
    </View>
  )
}

const styles = StyleSheet.create({
  aboutWrapper: {
    flex: 1,
    backgroundColor: '#fff',
  },

});

export default Search2
